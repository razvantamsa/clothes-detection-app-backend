service: clothes-detection-scraper
frameworkVersion: '3'

plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs18.x
  profile: clothes-detection-app
  region: us-west-2
  logRetentionInDays: 14
  timeout: 30
  apiGateway:
    binaryMediaTypes:
      - 'multipart/form-data'
  environment: ${file(./serverless/environment.yml):dev}
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:*
        - s3:*
        - lambda:*
        - secretsmanager:*
        - ses:*
      Resource: "*"

functions:
  start: ${file(./serverless/lambda/scraper.yml):start}
  integration: ${file(./serverless/lambda/scraper.yml):integration}
  productCatalog: ${file(./serverless/lambda/scraper.yml):productCatalog}
  productDetail: ${file(./serverless/lambda/scraper.yml):productDetail}

custom:
  webpack:
    webpackConfig: ./webpack.config.js
    includeModules:
      forceExclude:
        - aws-sdk 