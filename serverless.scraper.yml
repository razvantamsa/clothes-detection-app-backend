service: clothes-detection-scraper
frameworkVersion: '3'

plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs18.x
  profile: clothes-detection-app
  region: us-west-2
  logRetentionInDays: 14
  timeout: 30
  apiGateway:
    binaryMediaTypes:
      - 'multipart/form-data'
  environment: ${file(./serverless/environment.yml):dev}
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:*
        - s3:*
        - lambda:*
        - secretsmanager:*
        - ses:*
      Resource: "*"

functions:
  start: ${file(./serverless/lambda/scraper.yml):start}
  integration: ${file(./serverless/lambda/scraper.yml):integration}
  productCatalog: ${file(./serverless/lambda/scraper.yml):productCatalog}

  productDetail-1: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-2: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-3: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-4: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-5: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-6: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-7: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-8: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-9: ${file(./serverless/lambda/scraper.yml):productDetail}
  productDetail-10: ${file(./serverless/lambda/scraper.yml):productDetail}

custom:
  webpack:
    webpackConfig: ./webpack.config.js
    includeModules:
      forceExclude:
        - aws-sdk 